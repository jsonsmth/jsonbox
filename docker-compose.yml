version: '2'
services:
  jsonbox:
    container_name: jsonbox
    restart: always
    environment:
      - MONGODB_URI=mongodb://mongo:27017/jsonbox-io-dev
    build: .
    ports:
      - '3001:3000'
